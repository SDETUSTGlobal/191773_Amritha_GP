Import ( browser, element, by from "protractor";

describe("Login Demo, ()-> (

browser.wait For Angular Enabled (false); it("Protractor Login', ()-> {

browser.get(http://secure.smartbearsoftware.com/samples/TestCompletell/Meborders/login.aspr")

.then(() element (by.id('ctlee_MainContent_username')).sendKeys (Tester')) the keyword .then(()-> element (by.id(ct100_MainContent password")).sendkeys (test'))

.then(() → element (by.name('ctleesmainContent$login_button")).click())

.then(()->getAttribute("title"))

